import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: 86,
    height: 86,
    margin: 10,
    borderRadius: 40,
    borderWidth: 3,
    borderColor: '#ae22ea',
  },
  image: {
    width: 80,
    height: 80,
    borderRadius: 40,
    borderWidth: 3,
    borderColor: '#fff',
  },
});

export default styles;
