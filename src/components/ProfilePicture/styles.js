import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 7,
    borderRadius: 40,
    borderWidth: 3,
    borderColor: '#ae22ea',
  },
  image: {
    borderRadius: 40,
    borderWidth: 3,
    borderColor: '#fff',
  },
});

export default styles;
